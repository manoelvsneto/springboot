create sequence dbo.tutorials_seq start with 1 increment by 50;

    create table dbo.tutorials (
       id bigint not null,
        description varchar(255),
        published bit,
        title varchar(255),
        primary key (id)
    );
create sequence dbo.tutorials_seq start with 1 increment by 50;

    create table dbo.tutorials (
       id bigint not null,
        description varchar(255),
        published bit,
        title varchar(255),
        primary key (id)
    );
create sequence dbo.tutorials_seq start with 1 increment by 50;

    create table dbo.tutorials (
       id bigint not null,
        description varchar(255),
        published bit,
        title varchar(255),
        primary key (id)
    );
create sequence dbo.tutorials_seq start with 1 increment by 50;

    create table dbo.tutorials (
       id bigint not null,
        description varchar(255),
        published bit,
        title varchar(255),
        primary key (id)
    );
